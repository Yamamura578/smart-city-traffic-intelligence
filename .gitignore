# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Derived data — regenerated by the pipeline, not version controlled
data/processed/*
!data/processed/.gitkeep

# NOTE: logs/ is intentionally NOT ignored. A sample pipeline.log is a
# required deliverable. Likewise capstone_part3/mlruns/ — MLflow output is
# a required deliverable, so keep it committed.
.virtual_documents/
